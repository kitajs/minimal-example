{
  "name": "@kitajs/minimal-example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "kita build && tsc",
    "start": "node dist",
    "test": "kita build --dry-run && tsc --noEmit"
  },
  "devDependencies": {
    "@kitajs/cli": "^1.1.8",
    "@kitajs/ts-plugin": "^1.1.6",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@fastify/sensible": "^5.3.0",
    "@fastify/swagger": "^8.11.0",
    "@fastify/swagger-ui": "^1.10.1",
    "@kitajs/runtime": "^1.1.5",
    "fastify": "^4.24.2"
  }
}
