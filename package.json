{
  "name": "@kitajs/minimal-example",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "kita build && tsc",
    "start": "node dist",
    "test": "kita build --dry-run && tsc --noEmit"
  },
  "devDependencies": {
    "@kitajs/cli": "^1.1.10",
    "@kitajs/ts-plugin": "^1.1.8",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@fastify/sensible": "^5.4.0",
    "@fastify/swagger": "^8.12.0",
    "@fastify/swagger-ui": "^1.10.1",
    "@kitajs/html": "^3.0.4",
    "@kitajs/runtime": "^1.1.6",
    "fastify": "^4.24.2"
  }
}
